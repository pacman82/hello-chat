[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = [
    "rt-multi-thread",
    "macros",
    "sync",
    "signal",
] }
tokio-tungstenite = "0.27"
futures-util = "0.3"
chat = { path = "../chat" }
